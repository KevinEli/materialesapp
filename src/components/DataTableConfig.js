const paginationOptions =
{
    rowsPerPageText: 'Filas por Página:',
    rangeSeparatorText: 'de',
    selectAllRowsItem: true,
    selectAllRowsItemText: 'Todos',
    noDataComponent:'No hay registros para mostrar'
};

const paginationPerPage = 5;

const paginationRowsPerPageOptions = [5, 10, 15, 20, 25, 30];

const fixedHeaderScrollHeight = "600px";

export { paginationOptions, paginationPerPage, paginationRowsPerPageOptions, fixedHeaderScrollHeight };